---
name: GitHub custom action sample
description: Github custom action sample using Docker and Go
inputs:
  foo-input:
    description: "bar"
outputs:
  hoge-output:
    description: "fuga"
runs:
  using: docker
  image: Dockerfile
  args:
    - ${{inputs.foo-input}}
